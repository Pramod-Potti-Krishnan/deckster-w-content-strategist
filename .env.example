# Deckster Environment Variables

# Application Settings
APP_ENV=development
DEBUG=true
LOG_LEVEL=INFO

# API Settings
API_HOST=0.0.0.0
PORT=8000  # Railway will auto-set this

# Supabase Configuration (REQUIRED - Application will not start without these)
SUPABASE_URL=https://your-project.supabase.co  # Required: Get from Supabase project settings
SUPABASE_ANON_KEY=your-anon-key-here  # Required: Get from Supabase project API settings
SUPABASE_SERVICE_KEY=your-service-key-here  # Optional but recommended for admin operations

# AI Services (At least one required)
GOOGLE_API_KEY=  # Get from https://aistudio.google.com/apikey
OPENAI_API_KEY=sk-...your-key-here
ANTHROPIC_API_KEY=sk-ant-api03-...your-key-here  # Alternative to OpenAI

# Logging & Observability (Optional but Recommended)
LOGFIRE_TOKEN=your-logfire-write-token  # Get from https://logfire.pydantic.dev
LOGFIRE_ENVIRONMENT=development  # development, staging, or production
LOGFIRE_CONSOLE=false  # Set to true for console output during development
LOGFIRE_TRACE_SAMPLE_RATE=1.0  # 1.0 = 100% sampling (reduce in production)
LOGFIRE_SEND_TO_LOGFIRE=true  # Set to false to disable sending to Logfire

# Layout Architect Settings (Phase 2)
LAYOUT_ARCHITECT_MODEL=gemini-2.5-flash-lite-preview-06-17  # AI model for layout generation
LAYOUT_ARCHITECT_TEMPERATURE=0.7  # Creativity level (0.0-1.0)
LAYOUT_GRID_WIDTH=160  # Grid width in units
LAYOUT_GRID_HEIGHT=90  # Grid height in units
LAYOUT_MARGIN=8  # Minimum margin in grid units
LAYOUT_GUTTER=4  # Space between containers in grid units
LAYOUT_WHITE_SPACE_MIN=0.3  # Minimum white space ratio (30%)
LAYOUT_WHITE_SPACE_MAX=0.5  # Maximum white space ratio (50%)

# Three-Agent Layout Architect Configuration (Phase 2 - New Architecture)
THEME_AGENT_MODEL=gemini-1.5-flash  # Theme Agent (Brand & Style Director)
STRUCTURE_AGENT_MODEL=gemini-1.5-flash  # Structure Agent (Content Strategist)
LAYOUT_ENGINE_MODEL=gemini-1.5-flash  # Layout Engine (Master Artisan)

# Phase 2B Content-Driven Architecture Configuration
USE_PHASE_2B_ARCHITECTURE=true  # Enable Phase 2B (recommended for better structure preference adherence)
CONTENT_AGENT_MODEL=gemini-1.5-flash  # Content Agent (Content Preparation Specialist)
USE_LEGACY_WORKFLOW=false  # Use legacy 3-agent workflow in Phase 2A (not recommended)

# Notes:
# - Copy this file to .env and fill in your actual values
# - Never commit .env to version control
# - All SUPABASE_* values come from your Supabase project settings
# - At least one AI service key (Google, OpenAI or Anthropic) is required